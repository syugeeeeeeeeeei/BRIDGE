# v0.11.0 実装・検証結果

## 実装済み

- BRIDGE本体を `src/bridge/` 配下へ移行
- 正式CLIを `src/products/cli/cmd/bridge/` に配置
- 旧開発用CLIを `others/legacy/cmd-v0.10/` へ移動
- GATE公開DTO、公開Router、公開エラーを追加
- `bridge route` のstdin／`--request`入力を実装
- stdout／`--output`出力と`--overwrite`制御を実装
- 未知フィールド、重複JSONキー、複数JSON値の拒否を実装
- inline／fileグラフ入力、node／edge／weight検証を実装
- Route Result v1契約を実装
- Observation ModeとEvent Sinkを分離
- DiscardSink、MemorySink、WriterSink、CallbackSink、MultiSinkを実装
- Route Request JSON Schemaと実行例を追加

## 検証結果

以下をすべて正常完了した。

```text
go test ./...
go vet ./...
go build ./src/products/cli/cmd/bridge
bridge route --request tests/examples/route-request.json
```

CLIスモークテストでは `bridge.route.result.v1` のJSON結果がstdoutへ出力され、距離3、経路 `[0,1,2]` を取得した。

## 次段階

改善方針の段階計画に従い、Scenario駆動Benchmark、FileSinkの新Sink APIへの完全移行、Python／TypeScript SDK、APIサーバーは後続版の対象とする。

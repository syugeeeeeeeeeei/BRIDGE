# ANCHOR Go移植報告 v0.2.0

**状態:** 履歴報告

## 概要

geometric corridor、hub-aware、portal、weighted-cost、local repairを追加し、単一weighted A*から複数strategy型ANCHORへ移行した。

## 現行仕様との関係

本報告は当時の実装状態を記録する。現行の規則は`docs/ARCHITECTURE_RULE.md`、`docs/WORD_DEFINITION.md`、各`COMPONENT_RULE.md`を優先する。
